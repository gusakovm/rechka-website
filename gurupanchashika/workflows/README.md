# Workflows для Gurupanchashika

Утилиты и скрипты для работы с проектом.

## Скрипты

### 🎵 convert-audio.sh

Конвертация аудио файлов из OGG в MP3 для максимальной совместимости с браузерами.

**Использование:**
```bash
cd workflows
./convert-audio.sh
```

**Требования:**
- `ffmpeg` (уже установлен)

**Как это работает:**
- Скрипт сравнивает файлы в `audio-source/` с `audio-mp3/` по именам
- Конвертирует только те OGG, для которых нет соответствующих MP3
- Уже сконвертированные файлы не трогает (пропускает)

**Структура директорий:**
- `gurupanchashika/audio-source/` — исходные OGG файлы
- `gurupanchashika/audio-mp3/` — конвертированные MP3 файлы

### 🎙️ transcribe-audio.sh

Транскрибация аудио файлов в текст через webhook API.

**Использование:**
```bash
cd workflows
./transcribe-audio.sh
```

**Как это работает:**
- Берет все MP3 файлы из `audio-mp3/`
- Отправляет каждый файл на webhook API
- Получает транскрибацию и сохраняет в `transcriptions/`
- Пропускает файлы, которые уже транскрибированы

**Требования:**
- `curl`
- MP3 файлы должны быть уже сконвертированы

**Структура:**
- `gurupanchashika/audio-mp3/` — исходные MP3 файлы
- `gurupanchashika/transcriptions/` — текстовые транскрибации

### 🚀 start-server.sh

Запуск локального веб-сервера для разработки всего проекта.

**Использование:**
```bash
cd workflows
./start-server.sh
```

**Доступные URL:**
- `http://localhost:8000/` — корень проекта
- `http://localhost:8000/gurupanchashika/` — директория gurupanchashika
- `http://localhost:8000/gurupanchashika/index.html` — файлы проекта

**Требования:**
- Python 3

## Примечания

- Все скрипты используют относительные пути и могут быть запущены откуда угодно
- MP3 файлы не перезаписываются — если файл уже существует, он будет пропущен
- Конвертация использует VBR качество ~190 kbps для оптимального баланса размер/качество
